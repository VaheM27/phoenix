language: node_js
node_js:
  - 16.15.1
cache: 
  - node_modules
  - yarn

install:
  - yarn install

script:
  - yarn start build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: main

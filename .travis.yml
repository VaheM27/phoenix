language: node_js
node_js:
  - 16.15.1
cache: 
  - node_modules
  - ios/Pods

install:
  - yarn install

script:
  - yarn lint

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: main
